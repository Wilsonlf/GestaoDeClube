<div class="admin-topbar">
  <div class="admin-user">
    <div class="avatar"><%= (usuarioLogado || 'A').charAt(0).toUpperCase() %></div>
    <span><%= usuarioLogado || 'Admin' %></span>
  </div>
</div>
